#ifndef DIRECTUSAUDIOSOURCE_H
#define DIRECTUSAUDIOSOURCE_H

#include <QWidget>

class DirectusAudioSource : public QWidget
{
    Q_OBJECT
public:
    explicit DirectusAudioSource(QWidget *parent = 0);

signals:

public slots:
};

#endif // DIRECTUSAUDIOSOURCE_H